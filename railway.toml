[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/app/Dockerfile"
watchPatterns = ["packages/app/**", "api/**", "backend/**"]

[deploy]
startCommand = "npm run start:railway"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
